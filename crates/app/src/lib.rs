//! Библиотека приложения-терминала.
//!
//! Здесь живёт оркестрация: код, связывающий сетевой источник данных
//! (`data::MarketData`) с локальным хранилищем (`storage::Db`). Источник
//! абстрактный — реальный gRPC-клиент Finam (Фаза 0) и моки в тестах
//! подставляются через дженерик, поэтому оркестрация собирается и тестируется
//! кросс-платформенно.
//!
//! В Фазе 3 сюда добавляется [`api`] — view-model для фронта (контракт
//! Tauri-команд), не зависящий от самого `tauri`, чтобы собираться в CI.

pub mod api;
pub mod ingest;
