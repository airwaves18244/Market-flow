//! Сборочный скрипт. Генерацию контекста Tauri запускаем только при включённой
//! фиче `tauri` — в обычной (кросс-платформенной) сборке это no-op, и
//! опциональная build-зависимость `tauri-build` не подтягивается.

fn main() {
    #[cfg(feature = "tauri")]
    tauri_build::build();
}
