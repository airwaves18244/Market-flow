# Task List — Market Terminal

Детальный список задач для имплементации, выведенный из [`ROADMAP.md`](./ROADMAP.md).
Каждая задача — атомарная единица работы с критерием готовности (DoD).
Нумерация подпунктов (§ 0.1, § 1.3, …) совпадает с фазами в `ROADMAP.md`.

Легенда статуса: `[ ]` — не начато · `[~]` — в работе · `[x]` — готово.

Условные обозначения слоёв: **`finam-proto`** · **`data`** · **`storage`** ·
**`domain`** · **`app`** · **`frontend`**.

---

## Фаза 0 — Фундамент (завершение)

> Workspace, дисциплина слоёв, контракты `data`, DDL и классификация секторов
> уже сделаны. Остаётся сетевой каркас gRPC.

### 0.1 gRPC-стабы из `.proto` (`finam-proto`)
- [ ] Добавить `.proto` Finam (`FinamWeb/trade-api-docs`) в `crates/finam-proto/proto/`
      (vendored, с фиксацией версии/коммита источника в README крейта).
- [ ] Подключить `tonic` + `prost` в зависимости, `tonic-build` в `[build-dependencies]`.
- [ ] Написать `build.rs`: компиляция `AuthService`, `AssetsService`,
      `MarketDataService` (без `AccountsService`).
- [ ] Реэкспортировать сгенерённых клиентов/сообщения через `tonic::include_proto!`.
- [ ] **DoD:** `cargo build -p finam-proto` собирается, типы клиентов доступны из `data`.

### 0.2 Транспорт и авторизация (`data`)
- [ ] TLS-канал через `rustls` к `ENDPOINT` (HTTP/2).
- [ ] `AuthService`: получение JWT по API-ключу, авто-refresh до истечения.
- [ ] `keyring`: чтение/запись API-ключа в ОС-хранилище (не в репозитории/файлах).
- [ ] Перехватчик (interceptor), добавляющий токен в метаданные каждого вызова.
- [ ] **DoD:** unit-тесты на логику refresh (с замоканными часами); ключ не попадает в логи.

### 0.3 Rate-limit и устойчивость (`data`)
- [ ] Per-method limiter на `governor` (≤200 запросов/мин на метод).
- [ ] Обработка `MaintenanceWindow` (05:00–06:15 MSK) → `DataError::MaintenanceWindow`.
- [ ] Авто-reconnect стримов (обрыв ~раз в 24 ч) с backoff.
- [ ] `tracing`: инструментирование вызовов, спаны на запрос/стрим.
- [ ] **DoD:** тест, что limiter блокирует > N запросов; reconnect восстанавливает стрим.

### 0.4 Реализация трейта `MarketData` (`data`)
- [ ] `assets(mic)` → `Vec<Instrument>` (маппинг `AssetsService.Assets`).
- [ ] `bars(symbol, tf, from, to)` → `Vec<Bar>` (`MarketDataService.Bars`).
- [ ] `last_quote(symbol)` → `Quote` (`MarketDataService.LastQuote`).
- [ ] `latest_trades(symbol)` → `Vec<Trade>` (`MarketDataService.LatestTrades`).
- [ ] Маппинг `TimeFrame` ↔ API-перечисление; нормализация времени в UNIX-сек UTC.
- [ ] **DoD:** интеграционный тест против фикстур ответов API (golden-файлы).

---

## Фаза 1 — Хранилище и ингест (`storage`)

### 1.1 Подключение DuckDB ✅
- [x] Добавить нативный `duckdb` (feature `bundled`) в зависимости `storage`.
- [x] Открытие/создание файла БД (`Db::open` / `open_in_memory`), конфиг пути.
- [x] Применение схемы через миграции при инициализации (идемпотентно).
- [x] **DoD:** тест создаёт БД в памяти/файле, применяет схему, проверяет версию.

### 1.2 Миграции ✅
- [x] Таблица `schema_migrations` (версия + применено_в).
- [x] Раннер миграций (`migrate::apply`): недостающие версии по порядку, атомарно.
- [x] Версия 1 = базовый DDL (`schema::ALL_DDL`).
- [x] **DoD:** повторное открытие не накатывает заново и не ломает схему.

### 1.3 Writer ингеста ✅
- [x] Upsert инструментов (`instruments`), ON CONFLICT по `symbol`.
- [x] Запись баров (`bars`) пачками, ON CONFLICT по PK `(symbol,timeframe,ts)`.
- [x] Запись снимков оборота (`turnover_snapshots`).
- [x] **DoD:** интеграционные тесты на идемпотентность upsert и батч-вставку.

### 1.4 Планировщик батч-поллинга (частично)
- [x] Примитив пакетного прохода `app::ingest::backfill_symbols` (дженерик над
      `MarketData`), устойчивый к сбою отдельного символа (`PollSummary`).
- [x] **DoD (примитив):** проход против мок-`MarketData` наполняет БД, ошибки
      по символам собираются, а не прерывают проход.
- [ ] Конфиг (список инструментов/ТФ, интервал) и фоновый цикл по расписанию.
- [ ] Уважение rate-limit/техокна — приходит с реальным `data` (Фаза 0).

### 1.5 Загрузка классификации секторов ✅
- [x] Таблица `sector_map`: `upsert_sector_map` (детект ISIN по форме).
- [x] Проставление `sector` в `instruments` (`apply_sectors_to_instruments`),
      приоритет тикера над ISIN — на чистом SQL.
- [x] **DoD:** известные тикеры (SBER, GAZP) получают корректный сектор; тест
      на приоритет тикера над ISIN.
- [ ] Сид составов индексов MOEX (CSV/JSON) — данные подключаются отдельно.

### 1.6 Бэкфилл истории (частично)
- [x] Функция бэкфилла баров за диапазон `app::ingest::backfill_bars`
      (дженерик над `MarketData`), идемпотентна на уровне PK хранилища.
- [x] **DoD (функция):** повторный бэкфилл окна не плодит дубликаты (тест).
- [ ] Пагинация и возобновляемость — с реальным `data`/gRPC (Фаза 0).

### 1.7 Аналитические запросы ✅
- [x] «Оборот по секторам за период» (`turnover_by_sector`).
- [x] «Топ-движения» (`top_movers`, последний снимок на символ, QUALIFY).
- [x] Временные ряды нетто-потока (`net_flow_series`).
- [x] **DoD:** запросы покрыты интеграционными тестами на сид-данных.
- [ ] Скан «необычного объёма» из баров (есть в `domain`, нужен SQL-источник).

---

## Фаза 2 — Аналитика (`domain`) ✅

> Реализовано и покрыто тестами: turnover, flow (MFI/CVD), breadth, sector
> rollups, RRG, cross-asset shares + flow matrix. Открытые задачи —
> поддерживающие.
- [x] turnover / directional turnover / unusual volume.
- [x] money flow / MFI / CVD.
- [x] breadth (A/D, % растущих).
- [x] sector rollups (взвешенные по обороту).
- [x] RRG (RS-Ratio / RS-Momentum, квадранты).
- [x] cross-asset shares + flow matrix (Sankey).
- [ ] Property-based тесты на инварианты метрик (доли ∑=1, знаки потоков).
- [ ] Бенчмарки горячих путей (`criterion`) на крупных наборах баров.

---

## Фаза 3 — Tauri-оболочка + каркас фронта (`app` + `frontend`)

> Примечание: реальный рантайм `tauri` требует системного webkit2gtk, которого
> нет в headless-CI, поэтому в воркспейс он **не** добавлен (CI на Linux остаётся
> зелёным). Реализована независимая от `tauri` часть — view-model (§ 3.2) и
> каркас фронта (§ 3.4); сам бинарь Tauri собирается на десктоп-цели.

### 3.1 App state и оркестрация (`app`) (частично)
- [x] Lib-таргет `app` + модуль оркестрации (`app::ingest`), связывающий
      `MarketData` и `storage::Db`.
- [ ] Поднять Tauri-приложение (десктоп-цель с webkit).
- [ ] Общее состояние: соединение `data`, дескриптор `storage`, планировщик.
- [ ] **DoD:** приложение запускается, открывает окно, инициализирует слои.

### 3.2 IPC-команды (снимки + временные ряды) (частично)
- [x] View-model `app::api`: `equity_dashboard` (секторы + топ-движения),
      `flow_series` — сериализуемые функции поверх `storage` (без `tauri`).
- [x] Формы DTO синхронизированы с `frontend/src/lib/types.ts`.
- [x] **DoD:** функции покрыты тестами на сид-данных (валидный снимок/ряд).
- [ ] Тонкие `#[tauri::command]`-обёртки на десктоп-цели; единый формат ошибок IPC.
- [ ] Команды для breadth/RRG.

### 3.3 События (live-push)
- [ ] Emit Tauri-событий при обновлении котировок/сделок из стримов.
- [ ] Подписка/отписка фронта на каналы инструментов.
- [ ] **DoD:** мок-стрим доставляет события в окно.

### 3.4 Каркас фронта (`frontend`) ✅
- [x] Vite + Svelte 5 + TS проект под `frontend/` (собирается, типизирован).
- [x] Зависимости: `echarts`, `dockview-core`, `@tauri-apps/api`.
- [x] Тёмная тема; типизированная обёртка `invoke` (`ipc.ts`) с моками вне Tauri.
- [x] Панели представления «Акции»: treemap секторов, таблица топ-движений,
      линия нетто-потока (ECharts через Svelte-экшен).
- [x] **DoD:** `npm install`, `svelte-check` (0 ошибок), `vite build` — зелёные.
- [ ] Докуемые панели (dockview) вместо CSS-сетки; роутинг по 4 представлениям.
- [ ] `lightweight-charts`, `@tanstack/svelte-table` — по мере наполнения панелей.

---

## Фаза 4 — Представление 1: Акции / секторы (`frontend`)
- [ ] Treemap: размер = оборот, цвет = % изменения.
- [ ] Heatmap секторов/инструментов.
- [ ] Индикатор breadth (A/D, % растущих).
- [ ] Таблица топ-движений (TanStack Table, сортировка/виртуализация).
- [ ] RRG-чарт (квадранты, хвосты ротации).
- [ ] Подключение к IPC-командам Фазы 3.
- [ ] **DoD:** представление показывает живые/сид данные без ошибок консоли.

---

## Фаза 5 — Представления 2 и 3: Фьючерсы и Облигации

### 5.1 Фьючерсы
- [ ] Treemap по группам контрактов.
- [ ] Базис (фьючерс − спот) и его динамика.
- [ ] Терм-структура по экспирациям.
- [ ] Open interest (если доступен в API/данных).
- [ ] **DoD:** панель фьючерсов рендерит метрики из `domain`/`storage`.

### 5.2 Облигации
- [ ] Кривая доходности.
- [ ] Разбивка оборота по эмитентам/секторам.
- [ ] Обороты по выпускам.
- [ ] **DoD:** панель облигаций рендерит метрики из `domain`/`storage`.

---

## Фаза 6 — Представление 4: Сумма всех классов (`frontend`)
- [ ] Gauge общего оборота рынка.
- [ ] Donut долей по классам активов (cross-asset shares).
- [ ] Stacked area оборота во времени.
- [ ] Sankey перетоков капитала (flow matrix из `domain::crossasset`).
- [ ] **DoD:** сводное представление агрегирует все классы согласованно.

---

## Фаза 7 — Live-функции
- [ ] Стрим вотчлиста (свечи / стакан / лента) с авто-reconnect.
- [ ] Time & Sales (лента сделок).
- [ ] DOM (стакан заявок).
- [ ] Алёрты (правила по обороту/потоку/цене) + уведомления.
- [ ] Replay-режим (проигрывание исторических снимков из `storage`).
- [ ] **DoD:** live-панели обновляются в реальном времени; replay воспроизводит историю.

---

## Фаза 8 — Полировка и сборка
- [ ] Упаковка под Windows: MSI/NSIS через Tauri bundler.
- [ ] Профилирование и оптимизация горячих путей (ингест, рендер).
- [ ] Единообразная обработка ошибок и пользовательские сообщения.
- [ ] Экран настроек (API-ключ, пути, инструменты, интервалы поллинга).
- [ ] Документация пользователя + сборочная инструкция.
- [ ] **DoD:** установщик ставит приложение на чистой Windows и оно работает.

---

## Сквозные задачи (на протяжении всех фаз)
- [ ] CI: `cargo test --workspace` + `cargo clippy --workspace -D warnings` (Linux).
- [ ] CI: сборка/линт фронта (`npm run build`, `svelte-check`).
- [ ] Покрытие тестами адаптеров `data`/`storage` (golden-фикстуры API).
- [ ] Логи/`tracing` с уровнями, без утечки секретов.
- [ ] Поддержание `ROADMAP.md` и этого `task_list.md` в актуальном состоянии.
</content>
</invoke>
